gatoDeSchrodinger = "🐈";

if (gatoDeSchrodinger == "🐈") {

  for (let i = 0; i < 10; i++) {
    //--------- ⬇️ Modifica el código de abajo ⬇️ ------------//

    

    //--------- ⬆️ Modifica el código de arriba ⬆️ -----------//

    if (typeof gatoDeSchrodingüer === "undefined") {
      throw new Error("El gato de Schrödinger debería existir");
    }

    if (typeof gatoDeSchrodingüer !== "undefined") {
      throw new Error("El gato de Schrödinger no debería existir");
    }
  }
}

it('la variable "valor" debería existir fuera del if debido al uso de var 🤔', () => {
  expect(typeof valor).toBe("undefined");
});
